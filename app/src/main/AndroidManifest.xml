<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qoridanisa.asuper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <uses-permission android: name="android.permission.CALL.PHONE"></uses-permission>
        <uses-Permissin android: name="android.permission.READ.PHONE.STATE"></uses-Permissin>
        <uses-permission android: name="android.permission.SEND_SMS"></uses-permission>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Kontak"></activity>
    </application>

</manifest>